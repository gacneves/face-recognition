name = input('Type the name of the person to be used for training the classifier (0 to finish): ')
while name != '0':
    print(name)
    name = input('Type the name of the person to be used for training the classifier (0 to finish): ')